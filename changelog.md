# LNMP 2.3 变更摘要

归总修改及验证结论。

## 1. 安装与组件

### 修改

- 主线组件更新为 Nginx/OpenResty、PHP 8.0 至 8.5、MySQL 8.0/8.4、
  MariaDB 10.11/11.4/11.8、Apache 2.4 和 phpMyAdmin 5.2 系列。
- 清理停止安全维护的旧版 PHP、数据库和 Apache 入口。
- 支持数据库官方通用二进制与源码编译，OpenResty 支持官方仓库包与源码编译。
- 安装、附加组件、升级和卸载入口补齐非交互确认、非法参数、EOF 与真实退出码处理。
- 编译、安装、配置测试或服务启动失败时停止流程，不再继续部署不完整产物。

### 验证

- Debian 13 上完成 LNMP 主线安装及 MySQL、MariaDB 两个数据库分支验证。
- Debian 13 上完成 LAMP、LNMPA、OpenResty、Redis、Memcached 与 Pure-FTPd 定向验证。
- 安装脚本语法、lint、一致性和定向回归测试通过。

## 2. 下载与供应链

### 修改

- 组件下载统一使用上游官方 HTTPS 来源。
- 按组件接入静态或上游 SHA-256、PGP 签名和软件仓库 GPG 签名校验。
- 下载、签名或校验失败时删除无效文件并终止，不允许跳过错误继续安装。
- 固定 acme.sh 版本并关闭其自动升级；发布包生成和版本更新流程同步校验清单。

### 验证

- 覆盖正确文件、篡改文件、签名失败、下载中断和缺少校验条目的失败路径。
- 上游地址、版本变量、校验文件与发布流程完成一致性检查。

## 3. Web、PHP 与站点管理

### 修改

- Nginx 默认支持 HTTP/2、HTTP/3、stream、brotli、cache purge 和可选 Lua/fancyindex。
- default 站点默认不执行任意 PHP；phpinfo、缓存测试页和 phpMyAdmin 均改为显式开启。
- phpMyAdmin 移到网站根目录外，并使用随机访问路径和可启停的 Web 映射。
- 站点新增可关闭 PHP、独立错误日志、自定义配置区块、删除失败回滚和 HTTPS 配置继承。
- LNMPA/LAMP 收紧 PHP 文件匹配、目录访问和符号链接边界。
- 增加 Node/Go 应用的独立账号、systemd 托管、日志和生命周期管理。
- PHP-FPM socket 权限、运行目录、扩展安装和多版本 PHP 服务管理完成统一。

### 验证

- 覆盖站点增删、纯静态站点、PHP/PATH_INFO、伪静态、HTTPS、phpMyAdmin 和反向代理。
- Nginx、OpenResty、LAMP 与 LNMPA 配置语法和服务失败码完成定向验证。
- WordPress 首页、固定链接、REST、后台、PHP-FPM 和 Redis 对象缓存完成实测。

## 4. 数据库安全与可靠性

### 修改

- MySQL/MariaDB 默认仅监听回环地址，socket 统一放在 `/run/mysqld`。
- 数据库 root 密码使用安全随机源，避免终端和安装日志泄露。
- MySQL 8.4 使用上游默认认证，不再强制启用已废弃认证插件。
- 数据目录非空时默认拒绝重装；需显式确认后才允许整体迁移，失败时保留原数据。
- 数据库升级搬迁改为逐步检查和逆序回滚，不再清空非空数据目录。
- 数据库管理支持安全的增删、改密、导入和导出，并校验外部命令返回码。
- 导入和备份恢复增加 SQL 边界检查，默认拒绝跨库删除、账号、授权和文件写入语句。
- root 密码重置改用禁用网络的临时实例与私有 socket。

### 验证

- MySQL 8.4 与 MariaDB 11.8 的安装、登录、站点账号、导入导出和 phpMyAdmin 管理通过。
- 覆盖非空数据目录、搬迁失败、凭据错误、恶意 SQL、干净备份和恢复失败路径。
- 数据库完整升级仍要求使用者在可回滚环境执行并验证备份。

## 5. 服务、权限与防火墙

### 修改

- 服务管理优先使用 systemd，并保留无 systemd 环境的兼容入口。
- Nginx、PHP-FPM、Redis、Memcached、Pure-FTPd 等服务增加有限次数的异常自动拉起。
- 新增 `lnmp health` 响应探测、失败计数、熔断和通知。
- 新增 `lnmp perm` 权限基线、服务钩子、定时检查和按条目忽略机制。
- 防火墙规则使用独立 `inet lnmp` 表及专用 systemd 单元，不覆盖系统主规则文件。
- SSH 放行端口按实际监听探测，不修改 `sshd_config`；数据库和缓存端口默认阻断外部访问。
- Redis 改为 systemd 直接托管前台进程，并在启动前检查端口占用。

### 验证

- 覆盖正常启停、配置失败、端口占用、异常退出、自动拉起、熔断和状态不一致场景。
- 权限检查覆盖正常、告警、阻断启动、定时任务、忽略及卸载恢复。
- nftables 规则加载、重启恢复、自定义端口和卸载清理完成实测。

## 6. 备份、恢复与通知

### 修改

- 新增 `lnmp backup` 初始化、定时备份、单站点备份、批次列表、恢复和试恢复。
- 支持数据库与网站分别设置保留期，失败批次不会伪装成完整备份。
- 支持 SFTP、FTP/FTPS 异地上传和 age/GPG 加密。
- 远端上传增加目录改名、大小核对、错误传播和明文清理。
- Telegram 通知支持安装、升级、备份、健康与权限事件，并保护凭据文件权限。

### 验证

- 本地数据库和网站备份、覆盖恢复、试恢复、保留期及失败清理通过。
- SFTP、FTP/FTPS、age 和 GPG 完成真实链路验证。
- Telegram 文本、Markdown、错误降级和通知节流完成真实 API 验证。

## 7. 文档与持续集成

### 修改

- `HowtoGuides.md` 作为主要操作与故障排查手册，危险命令增加路径和影响范围校验。
- CI 覆盖 ShellCheck、脚本语法、一致性、版本更新、上游探测和发布包检查。

### 验证

- 文档命令已对照当前脚本入口、参数和帮助信息复核。
- README 与操作手册中的 Shell 代码块通过语法检查；只读状态和列表命令完成定向实测。
- 安装模式差异已复核：LNMP 使用 PHP-FPM，LNMPA/LAMP 使用 Apache 承载 PHP。
- 发布文件与内部日志职责分离，公开摘要不包含审计编号、内部环境或详细证据。
