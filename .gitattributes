# 换行符规范
#
# 本项目在 Windows 上开发、在 Linux 上运行，这是最容易出事的组合：
# 一个 shell 脚本只要混进 CRLF，在 Linux 上就会报
#     /bin/bash^M: bad interpreter: No such file or directory
# 或者更难查的 `command not found`（行尾的 \r 被当成命令的一部分）。
# 而这类文件在 Windows 编辑器里看起来完全正常。
#
# 所以统一规定：**仓库里存 LF，检出到工作区也是 LF**。
# eol=lf 里的 lf 是关键 —— 只写 text=auto 的话，Windows 检出仍是 CRLF，
# 本地用 Git Bash 直接跑 t/ 下的脚本照样会踩坑。
#
# Windows 11 自带的记事本、以及任何正经编辑器都能正常处理 LF 文件。

* text=auto eol=lf

# ---------------------------------------------------------------------------
# 明确标注为二进制：永远不做任何换行转换
# ---------------------------------------------------------------------------
*.gif   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.ico   binary
*.pdf   binary

# 下载缓存（已被 .gitignore 排除，这里兜底，防止误加时被破坏）
*.tar.gz  binary
*.tar.bz2 binary
*.tar.xz  binary
*.tgz     binary
*.zip     binary
*.bz2     binary
*.gz      binary
*.xz      binary

# ---------------------------------------------------------------------------
# PGP 密钥材料：保持字节原样
#
# 这些是验签用的公钥（nginx.org、OpenResty）。今天它们已经是 LF，
# 加 -text 是为了防止将来有人用某个编辑器另存成 CRLF 之后被静默改写 ——
# 密钥文件不该因为换行策略而变动一个字节。
# ---------------------------------------------------------------------------
*.asc -text

# ---------------------------------------------------------------------------
# 下面几类由上面的 * 规则统一转成 LF，单独列出是为了写清楚"为什么必须是 LF"
# ---------------------------------------------------------------------------

# 校验清单：sha256sum -c 逐行比对，行尾多个 \r 会让文件名对不上，
# 表现为"明明文件在、却报校验失败"。
src/checksums.sha256 text eol=lf

# 运维命令脚本（无扩展名，靠内容识别，这里显式声明更保险）
conf/lnmp   text eol=lf
conf/lnmpa  text eol=lf
conf/lamp   text eol=lf

# 补丁文件：patch 对上下文行逐字节匹配，被 patch 的源码都是 LF
*.patch text eol=lf
*.diff  text eol=lf

# init 脚本与 systemd unit
init.d/* text eol=lf

# ---------------------------------------------------------------------------
# GitHub 语言统计：changelog 与文档体量很大，不参与语言占比统计，
# 免得仓库被识别成"Markdown 项目"
# ---------------------------------------------------------------------------
*.md linguist-documentation
